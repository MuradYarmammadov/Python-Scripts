# Python-Scripts
A collection of custom Python scripts made by me

* **`mac_changer.py`** - Changes the MAC address of a specified network interface. Useful for enhancing privacy on public networks or bypassing basic MAC filtering.
* **`ARP_network_scanner.py`** - Scans a local subnet using ARP broadcast packets to discover and list the IP and MAC addresses of all active devices on the network.
